
// eslint-disable-next-line react/prop-types
const DisplayFullName = ({ fullName}) => {
    console.log("DisplayForm rendered");
  return <span>{fullName}</span>;
};

export default DisplayFullName;
